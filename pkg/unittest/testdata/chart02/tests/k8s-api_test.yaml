suite: Test empty template
templates:
  - configuration.yaml
  - hpa.yaml
tests:
  - it: should validate all kinds of assertions
    asserts:
      - isType:
          path: apiVersion
          type: string
      - isValidSchema: {}
